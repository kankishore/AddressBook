version: 0.2

phases:
  build:
    commands:
      - echo Build started on 'date'
      - mvn test
  post_build:
    commands:
      - echo Build completed on 'date'
      - mvn package
artifacts:
  files:
    - arn:aws:kms:ap-south-1:006655474157:alias/aws/s3
